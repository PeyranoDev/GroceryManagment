{
  "name": "grocery-management",
  "version": "0.0.0",
  "private": true,
  "description": "Grocery Management System - Monorepo",
  "scripts": {
    "prepare": "husky || true",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
